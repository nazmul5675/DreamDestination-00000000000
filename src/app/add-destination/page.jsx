import AddDestinationClient from "./AddDestinationClient";

export const metadata = {
    title: "Share a New Destination",
    description: "Contribute to our global travel community by sharing your favorite hidden gems and travel stories.",
};

export default function AddDestinationPage() {
    return <AddDestinationClient />;
}
