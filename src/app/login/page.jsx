import LoginClient from "./LoginClient";

export const metadata = {
    title: "Login",
    description: "Sign in to your DreamDestination account to manage your trips and share new destinations.",
};

export default function LoginPage() {
    return <LoginClient />;
}
